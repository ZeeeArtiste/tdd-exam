export function solveNQueensClassic(n: number): string[][] {
    if (n === 1) {
      return [["#"]];
    }

    // No solution for n=2
    if (n === 2) {
        return [];
      }

    return [];
  }
  